/**
 * Screenshot audit — captures every key page for visual review.
 *
 * Phase 10 boundary: the Veil-era page list referenced LGOIMA cases
 * and PNCC-seeded documents that no longer exist in the Umbra
 * Ministry-of-Demo seed. This spec is `test.fixme()`'d until Phase 11
 * deploys a populated demo environment with real screenshots.
 */
import { test } from "@playwright/test";

test.describe.fixme("Screenshot Audit (Phase 11 demo data)", () => {
  test("dashboard", async () => {
    // Phase 11 will populate this against the live Ministry of Demo deploy.
  });

  test("batches list", async () => {
    // Phase 11 will populate this against the live Ministry of Demo deploy.
  });

  test("admin retention page", async () => {
    // Phase 11 will populate this against the live Ministry of Demo deploy.
  });
});
